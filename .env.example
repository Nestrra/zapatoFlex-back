# Server
PORT=4000
NODE_ENV=development

# API
API_PREFIX=/api/v1

# Database (PostgreSQL)
# Opción A: variables sueltas (desarrollo local)
DB_HOST=localhost
DB_PORT=5432
DB_USER=usuario_db
DB_PASSWORD=your_password_here
DB_NAME=zapatposFlexDB

# Opción B: una sola URL (recomendado en producción, ej. Vercel + Neon)
# El backend usa DATABASE_URL si está definida; si no, usa DB_* de arriba.
# Para Neon en Vercel usa la URL con "pooler" (recomendada para serverless):
# DATABASE_URL=postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DBNAME?sslmode=require

# JWT (para rutas protegidas y rol ADMIN)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d