# Server
PORT=4000
NODE_ENV=development

# API
API_PREFIX=/api/v1

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=raulc
DB_PASSWORD=your_password_here
DB_NAME=zapatposFlexDB

# Opcional: usar una sola URL en lugar de las variables anteriores
# DATABASE_URL=postgresql://user:password@localhost:5432/zapatposFlexDB