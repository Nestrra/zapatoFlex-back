# Server
PORT=4000
NODE_ENV=development

# API
API_PREFIX=/api/v1

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=usuario_db
DB_PASSWORD=your_password_here
DB_NAME=zapatposFlexDB

# Opcional: usar una sola URL en lugar de las variables anteriores
# DATABASE_URL=postgresql://user:password@localhost:5432/zapatposFlexDB

# JWT (para rutas protegidas y rol ADMIN)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d